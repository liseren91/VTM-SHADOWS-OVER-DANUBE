Place clan avatar images here.

- File format: prefer optimized `.webp` or `.jpg` (≤ 500 KB each).
- Naming: `clanname-<optional-variant>.webp` (e.g., `brujah.webp`, `toreador-2.jpg`).
- Usage in code: reference as `/clan-portraits/<filename>` so Vite serves from `public`.

Example:
```ts
image: '/clan-portraits/brujah.webp';
```

